import { TierFormatterPipe } from './tier-formatter.pipe';

describe('TierFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TierFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
