import { SpiritFormatterPipe } from './spirit-formatter.pipe';

describe('SpiritFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpiritFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
